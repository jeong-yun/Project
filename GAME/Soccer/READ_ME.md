# 축구 게임 프로토타입 — 개발 문서

> 이 문서 하나로 프로젝트 전체(목표·설계·구현·실행 환경·테스트·한계·다음 단계)를 파악할 수 있도록 작성했습니다.
> 구현 파일: **`soccer.py`** (단일 파일, Python + Pygame)
> 의존성 파일: **`requirements.txt`**
> 마지막 검증: 헤드리스 시뮬 다회 실행으로 모든 규칙 이벤트 발생 및 무오류 확인, 렌더링/루프 코드 문법 검증 완료.

---

## 1. 목표

스탯(골 결정력·피지컬·속도 등)이 주어졌을 때 돌아가는 축구 게임을 만든다. 두 가지 플레이 방식을 **하나의 엔진**으로 지원한다.

- **사람 플레이 모드** — 사람이 한 선수를 직접 조작, 나머지 21명은 AI. 최대한 실사에 가깝게.
- **자동(매니저) 모드** — 22명 전부 AI로 경기가 자동 진행. 사람은 지켜보거나(향후) 전술만 지정.

핵심 관심사는 **"사람이 플레이할 때의 실사성"** 이며, 실사성의 대부분은 그래픽이 아니라 시뮬레이션 로직에 있다는 전제로 설계했다.

---

## 2. 기술 선택과 근거

| 항목 | 선택 | 근거 |
|---|---|---|
| 언어/라이브러리 | Python + Pygame | 빠른 프로토타이핑, 로직 검증에 적합 |
| 시점 | 2D 탑다운(위에서 내려다봄) | 구현 난이도 낮고 전술·물리 검증에 집중 가능 |
| 내부 상태 모델 | **2.5D** (피치 평면 x, y + 공 높이 z) | 렌더러와 분리 — 나중에 3D로 그래픽만 교체 가능 |
| 최종 목표 그래픽 | 3D(FIFA 스타일) | **시뮬레이션 검증 후** Ursina/Panda3D로 렌더만 교체 |

> **핵심 결정**: 경기는 평면(x, y) + 공의 높이(z)로 시뮬레이션한다. 이 상태 모델은 렌더러에 의존하지 않으므로, 지금은 탑다운으로 그리고 나중에 같은 엔진 위에 3D 렌더를 올릴 수 있다. **실사성의 90%는 시뮬레이션에 있고 폴리곤에 있지 않다.**

---

## 3. 아키텍처 — 한 엔진, 두 모드

설계의 중심은 **Intent(의도) 레이어**다. 매 프레임 모든 선수는 `Intent`(이동방향·스프린트·액션·파워)를 내놓고, 엔진이 그것을 **[스탯 + 물리 + 확률]** 로 해석해 상태를 갱신한다.

```
[입력 / AI]  →  Intent  →  [해석: 스탯 + 확률]  →  [물리 적분]  →  [규칙]  →  [렌더]
```

사람 입력은 "AI 대신 Intent를 만드는 또 하나의 브레인"일 뿐이다. 따라서 엔진·물리·규칙·렌더가 그대로 공유된다.

- **사람 플레이 모드**: 사람 컨트롤러 1명 + AI 컨트롤러 21명
- **자동 모드**: AI 컨트롤러 22명

이 분리 덕분에 두 모드를 한 코드베이스(`step()` 함수)로 돌린다.

### 데이터 모델

| 구조체 | 핵심 필드 | 설명 |
|---|---|---|
| `Stats` | pace, accel, agility, finishing, passing, physical, stamina | 0~99 능력치 |
| `Player` | team, role, base_fx/fy, stats, pos[x,y], vel, facing, stamina, pid, **active** | active=False면 퇴장 |
| `Ball` | pos[x,y,z], vel[3], owner, cooldown, last_team | z=높이, last_team=마지막 터치 팀 |
| `Intent` | move, sprint, action('pass'/'shoot'/'tackle'), power | 사람·AI 공통 출력 |
| `World` | players(22), ball, score, time, owner, controlled, offside_suspects, attack_team, cards, events | 경기 전체 상태 |

포메이션은 4-4-2(정규화 좌표). `home_pos()`가 공 위치에 따라 팀 라인 전체를 슬라이드시킨다(team shape).

---

## 4. 스탯 → 물리 파라미터 매핑

능력치를 그대로 쓰지 않고 물리량/확률로 변환한다. "스탯이 살아있다"는 느낌의 핵심.

| 스탯 | 반영되는 값 (코드 근사) |
|---|---|
| pace(속도) | 최대 속력 `4.5 + pace/99 × 4.5` m/s (스프린트 ×1.22) |
| accel(가속) | 가속도 `18 + accel/99 × 22` m/s² |
| agility(민첩) | 고속에서 방향전환 허용량(turn cap) `0.35 + 0.65 × agility/99` |
| finishing(결정력) | 슛 각도 오차 σ `(1 − finishing/99) × 11°` |
| passing(패스) | 패스 각도 오차 σ `(1 − passing/99) × 9°` |
| physical(피지컬) | 태클 승률 로지스틱 `0.2 + 0.6 × σ(0.08 × (수비−공격))` |
| stamina(스태미나) | 스프린트 소모율/회복, 모든 능력치에 fatigue 계수로 작용 |

추가 보정: 슛 오차는 **압박(3m 내 상대 수) ×4°**, **불균형(현재 속력 초과분) +α°** 만큼 커진다. 실패한 태클은 **접근 속도가 빠를수록 반칙 확률↑**.

---

## 5. 실사화 기법(구현됨)

1. **독립 물리 공** (`step_ball`) — 공은 발에 붙지 않는다. 소유 중에는 발 앞을 스프링처럼 추종(빠를수록 터치가 느슨해짐 = 터치감). 자유 볼은 중력·공기저항·바운스(반발계수)·구름저항을 받는다.
2. **선수 관성** (`step_player`) — 즉시 멈추거나 급회전 불가. 가속/감속 + 고속에서 방향전환 제한(민첩). 스태미나로 후반 능력 저하.
3. **결과 = 의도 + 스탯 + 상황 + 확률** — 슛/패스/태클 모두 결정론이 아니라 가우시안 오차·로지스틱 확률로 처리.
4. **오프더볼 AI** (`ai_intent` + `home_pos` + `separation`) — 진형 슬라이드, 최근접 1명 압박, 선수 간 분산(뭉침 방지), 공격수의 오프사이드 라인 유지.
5. **자동 선수 전환** (`select_controlled`) — 사람 모드에서 볼 소유 선수(또는 공에 가장 가까운 우리 팀 선수)로 조작 대상이 자동 전환(FIFA식).

---

## 6. 구현된 규칙

### 6.1 득점
공이 골라인을 넘고 골대 폭 안 + 크로스바(2.44m) 아래면 득점 → 킥오프.

### 6.2 오프사이드 (`offside_line` / `mark_offside` / `update_possession`)
- 공을 **차는 순간** 상대 **2nd-last 수비수**의 x좌표를 오프사이드 라인으로 잡는다.
- 그 라인보다 앞 + 공보다 앞 + 상대 진영에 있던 동료를 "용의자"로 표시.
- 용의자가 **그 패스를 처음 컨트롤**하면 오프사이드 → 수비팀 간접 프리킥.
- 세트피스(스로인·코너·골킥) 직후 첫 플레이는 오프사이드 면제.
- 화면: 공 가진 팀의 오프사이드 라인을 **주황 점선**으로 표시. AI 공격수는 이 라인을 넘지 않게 움직인다.

### 6.3 파울 · 카드 (`do_tackle` → `award_foul` → `give_card`)
- 태클 실패 시 반칙 확률(`0.35 + 접근속도 보정`).
- 박스 밖 → **프리킥**(상대 9.15m 밀어냄). 박스 안 → **페널티킥**.
- 카드: 레드 4%, 옐로 25%(반칙 기준). **옐로 2장 = 퇴장**.
- 퇴장 시 해당 팀은 인원이 줄고(HUD `11v10` 표시), AI/소유/렌더에서 모두 제외. 카드 받은 선수 옆에 노란 마크.

### 6.4 세트피스 (`rules`)
| 상황 | 판정 | 재개 |
|---|---|---|
| 사이드라인 아웃 | 스로인 | 마지막 터치 상대 팀 |
| 골라인 아웃 + 수비가 마지막 터치 | 코너킥 | 공격 팀, 가까운 코너 |
| 골라인 아웃 + 공격이 마지막 터치 | 골킥 | 수비 팀, 골에어리어 |

모든 세트피스는 해당 팀의 최근접 선수에게 자동으로 공을 넘겨 재개한다. 이벤트는 화면 중앙 배너로 잠깐 표시된다.

---

## 7. 실행 환경 및 의존성

### 7.1 요구 사항

| 항목 | 요구 / 권장 | 검증 버전 |
|---|---|---|
| Python | 3.8 이상 (f-string·dataclasses·`min(default=)` 사용) | **3.10.12** |
| pygame | 2.6 이상 | **2.6.1** (SDL 2.28.4 번들) |
| 그 외 모듈 | `sys`, `math`, `random`, `argparse`, `dataclasses` | 전부 **표준 라이브러리** (별도 설치 불필요) |
| OS | Windows / macOS / Linux | Linux(Ubuntu 22.04)에서 검증 |
| 하드웨어 | 일반 PC면 충분 (22 에이전트 60FPS, GPU 불필요) | — |

> 외부 의존성은 **pygame 하나뿐**입니다. pygame 설치 시 SDL 등 네이티브 백엔드가 휠(wheel)에 포함되어 함께 설치되므로 별도 SDL 설치는 필요 없습니다.

### 7.2 설치

```bash
# 방법 1) requirements.txt 사용
pip install -r requirements.txt

# 방법 2) 직접 설치
pip install pygame
```

가상환경 권장:

```bash
python -m venv .venv
# Windows:  .venv\Scripts\activate
# macOS/Linux:  source .venv/bin/activate
pip install -r requirements.txt
```

`requirements.txt` 내용:

```
pygame>=2.6
```

---

## 8. 실행 방법과 조작

```bash
python soccer.py
```

| 입력 | 동작 |
|---|---|
| WASD / 화살표 | 이동 |
| Shift | 스프린트(스태미나 소모) |
| Space | 패스(자동 조준) |
| K (꾹 눌렀다 떼기) | 슛 — 누른 시간만큼 파워 차징 |
| J | 태클 |
| M | 사람 ↔ 자동 모드 전환 |
| + / − | 자동 모드 시뮬 속도(×1~8) |
| R | 킥오프로 리셋 |
| Esc | 종료 |

---

## 9. 테스트 환경 및 검증

### 9.1 검증 환경

| 항목 | 값 |
|---|---|
| OS | Ubuntu 22.04 (헤드리스 Linux 샌드박스) |
| Python | 3.10.12 |
| pygame / SDL | 2.6.1 / 2.28.4 |
| 디스플레이 | 없음 — `SDL_VIDEODRIVER=dummy`, `SDL_AUDIODRIVER=dummy` |

### 9.2 헤드리스 스모크 테스트

디스플레이 없이 자동 시뮬을 N스텝 돌려 런타임 오류와 규칙 동작을 검증한다. `run_test()`가 점수·잔여 인원·이벤트 카운트를 출력한다.

```bash
# 화면 없이 6000스텝(=시뮬 100초) 자동 경기
python soccer.py --test 6000

# 완전 헤드리스(디스플레이 드라이버 더미)로 강제 실행
SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=dummy python soccer.py --test 12000
```

출력 예시:

```
TEST OK  score=[1, 2]  players=11v11  events={'FOUL': 6, 'GOAL': 3, 'GOAL KICK': 10, 'OFFSIDE': 3, 'THROW-IN': 2, 'YELLOW CARD': 1}  time=200s
```

### 9.3 검증 결과

- **무오류**: 12000스텝 × 다회 반복 실행에서 예외/크래시 없음.
- **규칙 발생 확인**: `GOAL`, `OFFSIDE`, `FOUL`, `PENALTY`, `YELLOW CARD`, `THROW-IN`, `GOAL KICK` 이벤트가 실제로 발생.
- **인원 정합성**: 퇴장이 없으면 `11v11` 유지, 발생 시 정상 감소.
- **문법 검증**: 렌더링/메인 루프(`draw`, `game_loop`) 코드를 `python -m py_compile`로 문법 확인.

> 참고: 헤드리스 테스트는 렌더링을 제외한 **시뮬레이션·규칙 로직 전체**를 그대로 실행합니다(같은 `step()` 사용). 그래픽 부분은 별도로 문법 검증했습니다.

---

## 10. 코드 맵 (`soccer.py` 탐색 가이드)

| 영역 | 함수/구조 | 역할 |
|---|---|---|
| 상수 | 상단 `L, W, GOAL_W, ...` | 피치 규격·물리 상수 |
| 데이터 | `Stats / Player / Ball / Intent / World` | 상태 모델 |
| 소유권 | `update_possession` | 최근접 선수 컨트롤 + 오프사이드 발동 |
| 물리 | `step_player`, `step_ball` | 관성 이동 / 공 물리·드리블 |
| 액션 | `kick`, `do_pass`, `do_shot`, `do_tackle` | 의도→결과(스탯+확률) |
| 규칙 | `rules`, `restart`, `penalty`, `offside_*`, `*_card`, `award_foul` | 득점·세트피스·파울·오프사이드 |
| AI | `ai_intent`, `move_to`, `separation`, `home_pos` | 오프더볼 행동 |
| 입력 | `human_intent`, `select_controlled` | 사람 조작 |
| 통합 | `step` | 1프레임 시뮬(사람·AI 공통) |
| 출력 | `draw`, `game_loop`, `run_test`, `main` | 렌더·메인 루프·테스트 |

---

## 11. 진행 이력

- **v1 (코어 엔진)**: Intent 레이어, 독립 물리 공, 선수 관성, 스탯 기반 슛/패스/태클, 스태미나, 오프더볼 AI, 사람/자동 두 모드, 기본 아웃-오브-바운드 복귀, 탑다운 렌더링. 헤드리스 테스트 통과.
- **v2 (규칙 정교화)**: 오프사이드(라인 판정), 파울·옐로/레드카드·퇴장, 프리킥/페널티, 스로인·코너·골킥 구분, 오프사이드 라인/이벤트 배너/인원수 HUD. 다회 시뮬로 GOAL·OFFSIDE·FOUL·PENALTY·YELLOW·THROW-IN·GOAL KICK 발생 및 무오류 확인.
- **문서화**: 본 개발 문서 + `requirements.txt` 작성. 실행 환경·의존성·테스트 환경 명시.

---

## 12. 알려진 단순화 / 한계

- **오프사이드**는 "첫 컨트롤" 기준이라 적극/소극 관여(interfering) 구분·패시브 오프사이드 없음.
- **골키퍼 선방/다이브 AI 없음** → 페널티는 거의 득점됨.
- 세트피스는 자동 재개(수동 조준·킥 루틴 없음), **어드밴티지 룰 없음**(반칙 즉시 정지).
- 경기 시간 종료 개념 없음(연속 진행), **교체·부상 없음**.
- 그래픽은 아직 2D 탑다운(상태는 2.5D라 3D 전환 준비됨).
- 슛 커브(마그누스)·헤더/공중 경합 미구현.

---

## 13. 다음 단계 (우선순위)

1. **GK 선방 로직** — 페널티/슛 긴장감의 핵심.
2. **어드밴티지 룰 + 적극관여 오프사이드** — 규칙 자연스러움.
3. **세트피스 루틴** — 코너/프리킥 시 박스 침투·키커 조준.
4. **게임패드(아날로그 스틱) 지원** — 사람 조작 실사성.
5. **전술/포메이션 설정 UI** — 자동 모드 매니저 경험.
6. **슛 커브·헤더·공중볼** — 표현력.
7. **3D 렌더 교체(Ursina/Panda3D)** — 같은 엔진 위에 그래픽만.

---

## 14. 한 줄 요약

스탯을 물리·확률로 변환하고, 사람/AI를 Intent 레이어로 통일해 **한 엔진으로 두 모드**를 돌리는 탑다운 축구 프로토타입. 핵심 규칙(오프사이드·파울/카드·세트피스)까지 동작하며, 상태가 2.5D로 설계되어 추후 3D 그래픽 교체가 열려 있다. 외부 의존성은 pygame 하나, 헤드리스 시뮬로 규칙·로직을 검증했다.
